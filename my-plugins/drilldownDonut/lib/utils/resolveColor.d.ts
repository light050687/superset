import { AccentKey, CategoryColorOverride } from '../types';
import { Tokens } from '../themeTokens';
export declare function resolveCategoryColor(categoryName: string, colorMap: CategoryColorOverride[] | undefined, tokens: Tokens, fallbackIndex: number): {
    accent: AccentKey;
    color: string;
};
//# sourceMappingURL=resolveColor.d.ts.map